const INITIAL_STATE = {
    currentUser: 'اصغر آقا'
}

const productReducer = (state = INITIAL_STATE, action) => {
    switch(action.type){
        default:
            return state
    }
}

export default productReducer